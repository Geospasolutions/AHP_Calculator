from setuptools import setup

setup(name = 'ahp_calculator',
        version='1.0.4',
        description='calculate the ahp',
        author='me',
        packages=['ahp_calculator'],
        zip_safe = False)